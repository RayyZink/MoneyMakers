openapi: "3.0.3"

info:
  title: "Money 2026 API"
  description: |
    API REST multi-utilisateurs de gestion de budget personnel.
    JWT Bearer requis sur toutes les routes sauf /auth/login et /auth/register.
    Les ressources non documentées ici (Tiers, Catégories, SousCategories) suivent le même pattern REST.
  version: "1.0.0"
  contact:
    name: "Équipe Arytech - IG2I Centrale Lille"

servers:
  - url: "http://localhost:3000/api/v1"
    description: "Développement local"

tags:
  - name: Auth
  - name: Utilisateurs
  - name: Comptes
  - name: Mouvements
  - name: Virements

security:
  - bearerAuth: []

paths:

  /auth/register:
    options:
      tags: [Auth]
      summary: "OPTIONS"
      security: []
      responses:
        "204": { description: "Allow: POST, OPTIONS" }
    post:
      tags: [Auth]
      summary: "Créer un compte utilisateur"
      security: []
      operationId: register
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UtilisateurCreation" }
      responses:
        "201":
          description: "Compte créé"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Utilisateur" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "409": { $ref: "#/components/responses/Conflict" }
        "500": { $ref: "#/components/responses/InternalError" }

  /auth/login:
    options:
      tags: [Auth]
      summary: "OPTIONS"
      security: []
      responses:
        "204": { description: "Allow: POST, OPTIONS" }
    post:
      tags: [Auth]
      summary: "Se connecter — retourne un token JWT"
      security: []
      operationId: login
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LoginRequest" }
      responses:
        "200":
          description: "Connexion réussie"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LoginResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "500": { $ref: "#/components/responses/InternalError" }

  /auth/logout:
    options:
      tags: [Auth]
      summary: "OPTIONS"
      responses:
        "204": { description: "Allow: POST, OPTIONS" }
    post:
      tags: [Auth]
      summary: "Se déconnecter"
      operationId: logout
      responses:
        "204": { description: "Déconnexion réussie" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /utilisateurs/{idUtilisateur}:
    options:
      tags: [Utilisateurs]
      summary: "OPTIONS"
      parameters:
        - $ref: "#/components/parameters/idUtilisateur"
      responses:
        "204": { description: "Allow: GET, PUT, OPTIONS" }
    get:
      tags: [Utilisateurs]
      summary: "Récupérer son profil"
      operationId: getUtilisateurById
      parameters:
        - $ref: "#/components/parameters/idUtilisateur"
      responses:
        "200":
          description: "Profil utilisateur"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Utilisateur" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [Utilisateurs]
      summary: "Modifier son profil"
      operationId: updateUtilisateur
      parameters:
        - $ref: "#/components/parameters/idUtilisateur"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UtilisateurMAJ" }
      responses:
        "200":
          description: "Profil mis à jour"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Utilisateur" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /utilisateurs/{idUtilisateur}/comptes:
    options:
      tags: [Comptes]
      summary: "OPTIONS"
      parameters:
        - $ref: "#/components/parameters/idUtilisateur"
      responses:
        "204": { description: "Allow: GET, POST, OPTIONS" }
    get:
      tags: [Comptes]
      summary: "Lister ses comptes"
      operationId: getComptes
      parameters:
        - $ref: "#/components/parameters/idUtilisateur"
      responses:
        "200":
          description: "Liste des comptes"
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Compte" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [Comptes]
      summary: "Créer un compte bancaire"
      operationId: createCompte
      parameters:
        - $ref: "#/components/parameters/idUtilisateur"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CompteCreation" }
      responses:
        "201":
          description: "Compte créé"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Compte" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /comptes/{idCompte}:
    options:
      tags: [Comptes]
      summary: "OPTIONS"
      parameters:
        - $ref: "#/components/parameters/idCompte"
      responses:
        "204": { description: "Allow: GET, PUT, DELETE, OPTIONS" }
    get:
      tags: [Comptes]
      summary: "Récupérer un compte"
      operationId: getCompteById
      parameters:
        - $ref: "#/components/parameters/idCompte"
      responses:
        "200":
          description: "Compte trouvé"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Compte" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [Comptes]
      summary: "Modifier un compte"
      operationId: updateCompte
      parameters:
        - $ref: "#/components/parameters/idCompte"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CompteMAJ" }
      responses:
        "200":
          description: "Compte mis à jour"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Compte" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [Comptes]
      summary: "Supprimer un compte"
      operationId: deleteCompte
      parameters:
        - $ref: "#/components/parameters/idCompte"
      responses:
        "204": { description: "Compte supprimé" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /comptes/{idCompte}/solde:
    options:
      tags: [Comptes]
      summary: "OPTIONS"
      parameters:
        - $ref: "#/components/parameters/idCompte"
      responses:
        "204": { description: "Allow: GET, OPTIONS" }
    get:
      tags: [Comptes]
      summary: "Solde historique à une date donnée"
      operationId: getSoldeHistorique
      parameters:
        - $ref: "#/components/parameters/idCompte"
        - name: date
          in: query
          schema: { type: string, format: date, example: "2026-01-31" }
      responses:
        "200":
          description: "Solde calculé"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SoldeResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /comptes/{idCompte}/mouvements:
    options:
      tags: [Mouvements]
      summary: "OPTIONS"
      parameters:
        - $ref: "#/components/parameters/idCompte"
      responses:
        "204": { description: "Allow: GET, POST, OPTIONS" }
    get:
      tags: [Mouvements]
      summary: "Lister les mouvements d'un compte"
      operationId: getMouvements
      parameters:
        - $ref: "#/components/parameters/idCompte"
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/limit"
        - name: dateDebut
          in: query
          schema: { type: string, format: date }
        - name: dateFin
          in: query
          schema: { type: string, format: date }
        - name: typeMouvement
          in: query
          schema: { type: string, enum: [D, C] }
      responses:
        "200":
          description: "Liste paginée des mouvements"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PageMouvements" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      tags: [Mouvements]
      summary: "Créer un mouvement"
      operationId: createMouvement
      parameters:
        - $ref: "#/components/parameters/idCompte"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MouvementCreation" }
      responses:
        "201":
          description: "Mouvement créé"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Mouvement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "422": { $ref: "#/components/responses/UnprocessableEntity" }

  /mouvements/{idMouvement}:
    options:
      tags: [Mouvements]
      summary: "OPTIONS"
      parameters:
        - $ref: "#/components/parameters/idMouvement"
      responses:
        "204": { description: "Allow: GET, PUT, DELETE, OPTIONS" }
    get:
      tags: [Mouvements]
      summary: "Récupérer un mouvement"
      operationId: getMouvementById
      parameters:
        - $ref: "#/components/parameters/idMouvement"
      responses:
        "200":
          description: "Mouvement enrichi (vue V_MOUVEMENT)"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MouvementDetail" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [Mouvements]
      summary: "Supprimer un mouvement"
      operationId: deleteMouvement
      parameters:
        - $ref: "#/components/parameters/idMouvement"
      responses:
        "204": { description: "Mouvement supprimé" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /virements:
    options:
      tags: [Virements]
      summary: "OPTIONS"
      responses:
        "204": { description: "Allow: GET, POST, OPTIONS" }
    post:
      tags: [Virements]
      summary: "Créer un virement (appelle la procédure creerVirement)"
      description: |
        Crée automatiquement les 2 mouvements (débit + crédit).
        Si les comptes appartiennent à 2 utilisateurs différents → virementInterUtilisateur = true.
      operationId: createVirement
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/VirementCreation" }
      responses:
        "201":
          description: "Virement créé"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Virement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/UnprocessableEntity" }

# ============================================================
# COMPONENTS
# ============================================================
components:

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  parameters:
    idUtilisateur:
      name: idUtilisateur
      in: path
      required: true
      schema: { type: integer, example: 1 }
    idCompte:
      name: idCompte
      in: path
      required: true
      schema: { type: integer, example: 1 }
    idMouvement:
      name: idMouvement
      in: path
      required: true
      schema: { type: integer, example: 1 }
    page:
      name: page
      in: query
      schema: { type: integer, minimum: 1, default: 1 }
    limit:
      name: limit
      in: query
      schema: { type: integer, minimum: 1, maximum: 100, default: 20 }

  schemas:

    Utilisateur:
      type: object
      properties:
        idUtilisateur: { type: integer, example: 1 }
        nomUtilisateur: { type: string, example: "Dupont" }
        prenomUtilisateur: { type: string, example: "Alice" }
        login: { type: string, example: "alice.dupont" }
        ville: { type: string, nullable: true }
        codePostal: { type: string, nullable: true, maxLength: 5 }
        dateHeureCreation: { type: string, format: date-time }

    UtilisateurCreation:
      type: object
      required: [nomUtilisateur, prenomUtilisateur, login, motDePasse]
      properties:
        nomUtilisateur: { type: string, maxLength: 50 }
        prenomUtilisateur: { type: string, maxLength: 50 }
        login: { type: string, maxLength: 50 }
        motDePasse: { type: string, minLength: 8 }
        ville: { type: string, nullable: true }
        codePostal: { type: string, maxLength: 5, nullable: true }

    UtilisateurMAJ:
      type: object
      properties:
        nomUtilisateur: { type: string }
        prenomUtilisateur: { type: string }
        ville: { type: string, nullable: true }
        codePostal: { type: string, nullable: true }

    LoginRequest:
      type: object
      required: [login, motDePasse]
      properties:
        login: { type: string, example: "alice.dupont" }
        motDePasse: { type: string, example: "MotDePasse123!" }

    LoginResponse:
      type: object
      properties:
        token: { type: string, example: "eyJhbGci..." }
        utilisateur: { $ref: "#/components/schemas/Utilisateur" }

    Compte:
      type: object
      properties:
        idCompte: { type: integer, example: 1 }
        descriptionCompte: { type: string, example: "Compte courant" }
        nomBanque: { type: string, example: "BNP Paribas" }
        montantInitial: { type: number, format: float, example: 1500.00 }
        dernierMontantCalcule: { type: number, format: float, example: 2340.50 }

    CompteCreation:
      type: object
      required: [descriptionCompte, nomBanque]
      properties:
        descriptionCompte: { type: string, maxLength: 50 }
        nomBanque: { type: string, maxLength: 50 }
        montantInitial: { type: number, format: float, default: 0.00 }

    CompteMAJ:
      type: object
      properties:
        descriptionCompte: { type: string }
        nomBanque: { type: string }

    SoldeResponse:
      type: object
      properties:
        idCompte: { type: integer }
        date: { type: string, format: date }
        solde: { type: number, format: float, example: 2340.50 }

    Mouvement:
      type: object
      properties:
        idMouvement: { type: integer, example: 42 }
        dateMouvement: { type: string, format: date }
        idCompte: { type: integer }
        idTiers: { type: integer, nullable: true }
        idCategorie: { type: integer, nullable: true }
        idSousCategorie: { type: integer, nullable: true }
        montant: { type: number, format: float, example: 45.90 }
        typeMouvement: { type: string, enum: [D, C] }

    MouvementDetail:
      description: "Correspond à la vue V_MOUVEMENT"
      type: object
      properties:
        idMouvement: { type: integer }
        dateMouvement: { type: string, format: date }
        descriptionCompte: { type: string }
        nomTiers: { type: string, nullable: true }
        nomCategorie: { type: string, nullable: true }
        nomSousCategorie: { type: string, nullable: true }
        montant: { type: number, format: float }
        typeMouvement: { type: string, enum: [D, C] }

    MouvementCreation:
      type: object
      required: [montant, typeMouvement]
      properties:
        dateMouvement: { type: string, format: date }
        idTiers: { type: integer, nullable: true }
        idCategorie: { type: integer, nullable: true }
        idSousCategorie: { type: integer, nullable: true }
        montant: { type: number, format: float, minimum: 0.01 }
        typeMouvement: { type: string, enum: [D, C] }

    PageMouvements:
      type: object
      properties:
        data:
          type: array
          items: { $ref: "#/components/schemas/MouvementDetail" }
        meta: { $ref: "#/components/schemas/PaginationMeta" }

    PaginationMeta:
      type: object
      properties:
        page: { type: integer }
        limit: { type: integer }
        total: { type: integer }
        totalPages: { type: integer }

    Virement:
      type: object
      properties:
        idVirement: { type: integer }
        idCompteDebit: { type: integer }
        idCompteCredit: { type: integer }
        montant: { type: number, format: float }
        dateVirement: { type: string, format: date }
        commentaire: { type: string, nullable: true }
        virementInterUtilisateur: { type: boolean }

    VirementCreation:
      type: object
      required: [idCompteDebit, idCompteCredit, montant]
      properties:
        idCompteDebit: { type: integer, example: 1 }
        idCompteCredit: { type: integer, example: 3 }
        montant: { type: number, format: float, minimum: 0.01 }
        dateVirement: { type: string, format: date }
        commentaire: { type: string, nullable: true }
        idCategorie: { type: integer, nullable: true }
        idSousCategorie: { type: integer, nullable: true }

    Erreur:
      type: object
      properties:
        code: { type: integer }
        message: { type: string }
        details: { type: string, nullable: true }

  responses:
    BadRequest:
      description: "400 — Requête invalide"
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Erreur" }
    Unauthorized:
      description: "401 — Token manquant ou invalide"
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Erreur" }
    Forbidden:
      description: "403 — Accès refusé"
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Erreur" }
    NotFound:
      description: "404 — Ressource non trouvée"
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Erreur" }
    Conflict:
      description: "409 — Ressource déjà existante"
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Erreur" }
    UnprocessableEntity:
      description: "422 — Erreur métier"
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Erreur" }
    InternalError:
      description: "500 — Erreur interne"
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Erreur" }
